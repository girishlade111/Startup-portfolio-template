# Plan: Remove Theme Debug Panel Completely

## Summary
Remove the ThemeTest/ThemeDebug component entirely from the codebase.

## Files to Modify

### 1. Delete entire file
- `components/dev/ThemeTest.tsx` - DELETE the file completely

### 2. Edit app/layout.tsx
**Line 14:** Remove import statement:
```typescript
import { ThemeTest } from "@/components/dev/ThemeTest";
```

**Line 98:** Remove JSX component usage:
```typescript
<ThemeTest />
```

## Verification Steps
1. Run `npm run dev` to start the development server
2. Verify no debug panel appears on the page in any state (dark/light, any scroll position)
3. Check browser console for any errors related to the removed component